# drunk-pulumi-azure-components

Pulumi `ComponentResource` library that wraps upstream `@pulumi/azure-native` (3.x) / `@pulumi/azuread` resources into higher-level, config-driven components. The point is to let developers describe *intent* through a typed `Args` object — `ResourceBuilder` plus the domain components under `src/<domain>/` — instead of hand-wiring individual Azure resources; naming, tagging, and RBAC conventions come from `src/common/` so callers don't have to think about them, while every `Args` interface still exposes enough surface to override or opt out per resource.

Hardening is real but **partial** — don't market it as blanket "secure by default" without checking the component. TLS 1.2, HTTPS-only, and Key Vault's RBAC-auth + soft-delete + purge-protection are hardcoded/defaulted secure. But `publicNetworkAccess` and network ACL `defaultAction` default to **`Enabled`/`Allow`** in `StorageAccount.ts`, `KeyVault.ts`, `AzSql.ts`, `MySql.ts`, `Postgres.ts`, and `Redis.ts` — private access only kicks in once the caller explicitly sets `network.privateLink: true` or `network.defaultAction`. Published from `bin/` after build.

## Project Layout

| Path | Purpose |
|------|---------|
| `src/base/` | Shared base classes — `ResourceBuilder`, `AppResources`, `types.ts`. Every component flows through these. |
| `src/{aks,apim,app,azAd,database,logs,services,storage,vault,vm,vnet}/` | Domain components grouped by Azure service. |
| `src/common/`, `src/helpers/` | Cross-cutting utilities (naming, tags, RBAC role IDs, network/IP math via `netmask`, PGP via `openpgp`). |
| `__tests__/` | Jest unit tests. Mirrors `src/` layout. |
| `pulumi-test/` | Integration fixture — exercises components against a real Pulumi stack. |
| `bin/` | Build output (`tsc` target). **Never hand-edit.** Repopulated by `pnpm build`. |
| `.tasks/` | Build/release scripts (`npm-package.ts`, `update-tsconfig.ts`, `generate-docs.ts`). |

## Scripts

| Command | What it does |
|---------|--------------|
| `pnpm build` | `update-tsconfig` → `tsc` → copy README + `PulumiPlugin.yaml` into `bin/`. |
| `pnpm fastBuild` | `tsc` only — skip tsconfig regeneration and copy step. |
| `pnpm test` | Jest. |
| `pnpm docs` | Regenerate component docs. |
| `pnpm update` | `npm-check-updates -u` + `pnpm install`. |

Package manager is **pnpm** (`packageManager: pnpm@11.3.0`). Do not run `npm install` or `yarn`.

## Conventions

- **Component shape**: every public component extends `pulumi.ComponentResource`, accepts a typed `Args` interface, registers child resources with `{ parent: this }`, and calls `this.registerOutputs(...)` at the end of the constructor. New components must follow this pattern — read a sibling under the same `src/<domain>/` folder before writing one.
- **Inputs / outputs**: use `pulumi.Input<T>` for constructor args and `pulumi.Output<T>` for exposed properties. Never `await` outputs — use `.apply()`.
- **Naming + tags**: pull from `src/common/` helpers; do not hard-code resource names or tag objects.
- **RBAC**: role definition IDs come from the helper map in `src/common/`/`src/azAd/`. Do not paste raw GUIDs into components.
- **Formatting**: Prettier config lives in `package.json` (`singleQuote`, `printWidth: 120`, `trailingComma: all`, `semi: true`). ESLint via `eslint.config.js`.
- **Tests**: Jest + `ts-jest`. Co-locate by mirroring `src/` path inside `__tests__/`. Test the public component contract (inputs → registered child resources + outputs), not internal helpers.
- **Imports**: TypeScript `6.0.3`, `tsconfig.json` is regenerated by `.tasks/update-tsconfig.ts` — do not edit `tsconfig.json` by hand for new component dirs; add the dir and re-run `pnpm run update-tsconfig`.

## Editing Rules

- **Before changing a component's public surface** (constructor args, registered outputs, exported types), run impact analysis (see GitNexus block below). Consumers in `pulumi-test/` and downstream stacks break silently otherwise.
- **Security-sensitive surface**: `src/vault/`, `src/azAd/`, anything touching `keyvault-*`, RBAC role assignments, network ACLs (`netmask`), or `openpgp`. Treat changes here as security-reviewable — no widening of IAM scope, no public endpoints, no plaintext secret handling without explicit ask.
- **Build output**: never edit `bin/`. If `bin/` looks wrong, fix the source and rebuild.
- **Lockfile**: mutate `pnpm-lock.yaml` only via `pnpm install` / `pnpm update`. Never hand-edit.
- **Azure SDK shapes shift between minor versions of `@pulumi/azure-native`**. When in doubt about a resource's property names or enum values, consult Microsoft Learn / Pulumi registry rather than guessing.

## References

- `README.md` — public-facing component catalog.
- `SECURITY.md` — disclosure policy.
- `.github/copilot-instructions.md`, `.github/COPILOT_GUIDELINES.md` — additional contributor guidance shared with Copilot.
- `doc/` — generated component documentation.

<!-- gitnexus:start -->
# GitNexus — Code Intelligence

This project is indexed by GitNexus as **drunk-pulumi-azure-components** (2326 symbols, 4914 relationships, 192 execution flows). Use the GitNexus MCP tools to understand code, assess impact, and navigate safely.

> If any GitNexus tool warns the index is stale, run `npx gitnexus analyze` in terminal first.

## Always Do

- **MUST run impact analysis before editing any symbol.** Before modifying a function, class, or method, run `gitnexus_impact({target: "symbolName", direction: "upstream"})` and report the blast radius (direct callers, affected processes, risk level) to the user.
- **MUST run `gitnexus_detect_changes()` before committing** to verify your changes only affect expected symbols and execution flows.
- **MUST warn the user** if impact analysis returns HIGH or CRITICAL risk before proceeding with edits.
- When exploring unfamiliar code, use `gitnexus_query({query: "concept"})` to find execution flows instead of grepping. It returns process-grouped results ranked by relevance.
- When you need full context on a specific symbol — callers, callees, which execution flows it participates in — use `gitnexus_context({name: "symbolName"})`.

## Never Do

- NEVER edit a function, class, or method without first running `gitnexus_impact` on it.
- NEVER ignore HIGH or CRITICAL risk warnings from impact analysis.
- NEVER rename symbols with find-and-replace — use `gitnexus_rename` which understands the call graph.
- NEVER commit changes without running `gitnexus_detect_changes()` to check affected scope.

## Resources

| Resource | Use for |
|----------|---------|
| `gitnexus://repo/drunk-pulumi-azure-components/context` | Codebase overview, check index freshness |
| `gitnexus://repo/drunk-pulumi-azure-components/clusters` | All functional areas |
| `gitnexus://repo/drunk-pulumi-azure-components/processes` | All execution flows |
| `gitnexus://repo/drunk-pulumi-azure-components/process/{name}` | Step-by-step execution trace |

## CLI

| Task | Read this skill file |
|------|---------------------|
| Understand architecture / "How does X work?" | `.claude/skills/gitnexus/gitnexus-exploring/SKILL.md` |
| Blast radius / "What breaks if I change X?" | `.claude/skills/gitnexus/gitnexus-impact-analysis/SKILL.md` |
| Trace bugs / "Why is X failing?" | `.claude/skills/gitnexus/gitnexus-debugging/SKILL.md` |
| Rename / extract / split / refactor | `.claude/skills/gitnexus/gitnexus-refactoring/SKILL.md` |
| Tools, resources, schema reference | `.claude/skills/gitnexus/gitnexus-guide/SKILL.md` |
| Index, status, clean, wiki CLI commands | `.claude/skills/gitnexus/gitnexus-cli/SKILL.md` |

<!-- gitnexus:end -->
